# Terraform working state — keep out of git, recreate with `terraform init`.
.terraform/
.terraform.lock.hcl
# Local state — fine for a single-operator setup. Move to S3 backend once a
# second person needs to apply.
*.tfstate
*.tfstate.*
# Per-environment overrides containing secrets / per-user values.
*.auto.tfvars
terraform.tfvars
